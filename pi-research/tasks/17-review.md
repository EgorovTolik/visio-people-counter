# Задача 17: Финальный код-ревью + мелкие фиксы + итоговая проверка

## Читать ПЕРЕД началом
1. /home/anatoliy/PiProjects/visio-people-counter/pi-research/05-development-plan.md — план, критерии приёмки.
2. Отчёты pi-research/11…16-task-report.md — что и как реализовано.
3. config.example.yaml — эталон схемы конфига.
4. Весь код: visio_people_counter/*.py, tests/*.py, __main__ (через visio_people_counter/__main__.py), README.md.

## Среда
ТОЛЬКО /home/anatoliy/PiProjects/visio-people-counter/.venv/bin/python. Ничего не устанавливать, ничего не коммитить.

## Роль: ты — ревьюер И исполнитель мелких фиксов (одним проходом)

### 1. Ревью по чек-листу
- **Соответствие ТЗ**: headless по умолчанию без окон и без ограничений скорости; --gui + --speed; calibrate;
  гибкие счётчики line/zone, both/total; фильтры вместо классификации; HLS+файл; watchdog; YAML-конфиг.
- **Корректность**: пересечение линии (отрезок prev→cur, направление, антидубль), зона, трекер (reset при reconnect,
  frame_rate), тени/морфология, размерные пороги; утечки ресурсов (cv2.VideoCapture.release, ffmpeg proc.kill/terminate,
  destroyAllWindows); обработка SIGINT; ошибки с понятными сообщениями и кодами возврата.
- **Безопасность/надёжность**: пути из конфига (без shell), ffprobe/ffmpeg — subprocess со списком аргументов (НЕ строкой!),
  JSONL flush/close, поведение при битом конфиге/отсутствующем файле/недоступном URL.
- **Качество**: дублирование кода, мёртвый код, несогласованность docstring/реальности, магические числа без имени,
  README соответствует реальному CLI (проверь фактические флаги argparse).
- **Консистентность с config.example.yaml**: все ключи конфига реально используются кодом; все возможности кода описаны в шаблоне.

### 2. Фиксы
Найденные проблемы исправляй НАПРЯМУЮ (мелкие и средние). Крупную архитектурную проблему НЕ переписывай —
зафиксируй в отчёте как ограничение/TODO. После фиксов:
- полный прогон `.venv/bin/python -m unittest discover -s tests` — все тесты зелёные;
- E2E-смоук: сгенерировать временный mp4 (как в тестах), прогнать `count` headless и `probe`;
  проверить, что при отсутствии дисплея `--gui` даёт понятную ошибку (в этом окружении его нет — проверь фактический вывод);
- README: довести до финального состояния (установка, все режимы с примерами, клавиши, структура проекта, параметры конфига → ссылка на config.example.yaml).

### 3. Итог
В отчёте: таблица найденных проблем (файл:строка, суть, фикс/TODO), финальный результат тестов, вывод E2E-смоука,
список того, что владельцу стоит проверить вручную на реальных видео (пошагово).

## Правила
- Все bash с timeout; без интерактивных команд; ничего не коммитить; references/, pi-research/tasks/ не трогать.

## Отчёт
/home/anatoliy/PiProjects/visio-people-counter/pi-research/17-review-report.md

## Формат ответа (СТРОГО)
ТОЛЬКО короткое резюме (10–20 строк: сколько проблем найдено/исправлено + результат тестов) + путь к отчёту.
